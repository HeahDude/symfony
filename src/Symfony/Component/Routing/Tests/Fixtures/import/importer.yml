routes:
    resource: imported.yml
    defaults:
        global: is_global
    requirements:
        global: should_be_global
    options:
        utf8: true
    condition: abc
    host: global_host
    schemes: https
    methods: GET
